(function (a) {
    a.fn.readmore = function (b) {
        function e(b) {
            b.append(d.more_link);
            b.find("." + d.more_clzz).click(function () {
                a(this).hide();
                b.find("." + d.ellipse_clzz).hide();
                b.find("." + d.hidden_clzz).animate({
                    opacity: "toggle"
                }, 1e3)
            })
        }
        function f(a) {
            var b = a.html();
            var c = "<span class='" + d.ellipse_clzz + "'>" + d.ellipses + "</span>";
            var remove_space =   jQuery.trim(b.substring())
            var e = remove_space.substring(0,20) + c;
            var f = '<span class="' + d.hidden_clzz + '" style="display:none;">' + b.substring(20) + "</span>";
            a.html(e + f)
        }
        var c = {
            substr_len: 300,
            split_word: false,
            ellipses: "&#8230;",
            more_link: '<a class="readm-more">Read More</a>',
            more_clzz: "readm-more",
            ellipse_clzz: "readm-continue",
            hidden_clzz: "readm-hidden"
        };
        var d = a.extend({}, c, b);
        this.each(function () {
            var b = a(this);
            if (jQuery.trim(b.html()).length > 110) {
                f(b);
                e(b)
            }
        });
        return this
    }
})(jQuery)